# Quickstart: Enterprise staging evidence

```powershell
cd BE_Bot_Auto_Trade_AI_Tu_Hoc\services\gateway
python scripts\export_phase2_evidence.py
```

Outputs under `artifacts/phase2-evidence/` (gitignored).  
Read runbook: `docs/shared/phase2-staging-runbook.md`.

**Does not** tick `release-gates.md` Pass.
