# Contracts — 004-binance-testnet-adapter

No new public REST/WS operations. Adapter is internal behind OMS (002 lock: no public create-order).

Reuses existing MVP ops: accounts, api keys, strategies activate, positions/pnl/reports.

If a public “venue health” op is needed later → RFC first.
